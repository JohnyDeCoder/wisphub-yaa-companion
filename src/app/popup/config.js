import { EXTENSION_GITHUB } from "../../config/constants.js";
import { ALLOWED_DOMAINS } from "../../config/domains.js";

export const POPUP_CONFIG = {
  GITHUB_URL: EXTENSION_GITHUB,
  DOMAINS: ALLOWED_DOMAINS,
  RETRY_DELAY: 5000,
  TOAST_DURATION: 3000,
};
